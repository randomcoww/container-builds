#!/usr/bin/with-contenv sh

juicefs format \
  --storage minio \
  --bucket $JFS_MINIO_BUCKET \
  --access-key $ACCESS_KEY_ID \
  --secret-key $SECRET_ACCESS_KEY \
  sqlite3://$JFS_DB_PATH \
  $JFS_RESOURCE_NAME